import React from "react";

const ErrorScreen = () => {
  return (
    <div style={{ width: "100%" }}>
      <p style={{ textAlign: "center" }}>
        Oopsie! Something's missing... An unknown error has occured, or the page
        you were looking for was loading incorrectly
      </p>
    </div>
  );
};

export default ErrorScreen;
